/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'source.unsplash.com',
      'oaidalleapiprodscus.blob.core.windows.net',
      'picsum.photos',
    ],
  },
};

module.exports = nextConfig; 